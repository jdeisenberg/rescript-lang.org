---
test: "foo"
---

Use `if / else` expressions to express a value through a `true` / `false` condition.

```res
let user = "Anna"

let ret = if user === "Anna" {
  "Hi Anna!"
}
else {
  "Hi unknown!"
}
```
